

const TimeLinePage =async () => {

    await new Promise((resolve) => setTimeout(resolve, 300));
    return (
        <div>
            Timeline
        </div>
    );
};

export default TimeLinePage;